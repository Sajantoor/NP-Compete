{
  "name": "binary-search-but-better-backend",
  "version": "0.0.1",
  "description": "binary search but better backend",
  "scripts": {
    "start": "node dist/index.js",
    "watch": "tsc -w",
    "dev": "nodemon dist/index.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.20"
  }
}